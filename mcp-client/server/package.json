{
  "name": "mcp-client-server",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Agent backend: connects to the address MCP server and drives a local Ollama model through tool calls.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "check": "node --check src/server.js && node --check src/agent.js && node --check src/ollama.js && node --check src/mcp.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  },
  "engines": { "node": ">=20" }
}
